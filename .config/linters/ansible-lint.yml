# purpose: Ansible-lint configuration for HybridOps Ansible collections.
# adr: ADR-0606-ansible-collections-release-process
# maintainer: HybridOps.Tech

profile: production

exclude_paths:
  - .git
  - .github
  - .tox
  - .venv
  - .mypy_cache
  - .pytest_cache
  - dist
  - build
  - output
  - tests/output
  - roles/.draft

skip_list:
  - yaml  # YAML style is enforced by yamllint via pre-commit.
  # HybridOps roles intentionally expose service-oriented variable contracts.
  # Enforcing a role-name prefix would create breaking churn without improving
  # runtime safety for the shipped collection surface.
  - var-naming[no-role-prefix]

warn_list:
  - no-changed-when
  - unnamed-task
  - fqcn-builtins
