---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: instance
    image: "rockylinux:9"
    pre_build_image: true
    privileged: true

provisioner:
  name: ansible
  playbooks:
    converge: converge.yml

verifier:
  name: ansible
  playbooks:
    verify: verify.yml
