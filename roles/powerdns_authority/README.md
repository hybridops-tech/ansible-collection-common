# powerdns_authority

Deploys and maintains the HybridOps PowerDNS authority service on the shared control host.

Use this role through the `platform/network/powerdns-authority` module or the
shared-primary blueprint path rather than calling it ad hoc.
